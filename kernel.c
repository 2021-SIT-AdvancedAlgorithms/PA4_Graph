#include "kernel.h"
#include <string.h>

vertex parse_adv_list(char* string) {
    // TODO : parse input
    vertex v;
    return v;
}

int find_islands(island** islands, vertex* vertices, int n_vertices) {
    // TODO : find islands

    // *islands = (island *) malloc(sizeof(island) * n_island);

    // return n_island;
}

int find_eulerian_path(int** vertexIndices, island island) {
    // TODO : find eulerian_path

    // *vertexIndices = (int*) malloc(sizeof(int*) * n_vertices_in_path);

    // return n_vertices_in_path;
    
}